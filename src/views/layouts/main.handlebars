<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title}}</title>

  <!-- CSS global -->
  <link rel="stylesheet" href="/css/styles.css" />

  <!-- JS global (inicializa carrito/contador en el navbar) -->
  <script src="/js/app.js" defer></script>
</head>
<body>
  <header class="site-header">
    <div class="wrap nav">
      <a class="brand" href="/"><span class="brand-top">Herramienta</span><span class="brand-bottom">del Sur</span></a>
      <nav class="links">
        <a href="/" class="{{#if (eq page 'home')}}active{{/if}}">Inicio</a>
        <a href="/productos" class="{{#if (eq page 'productos')}}active{{/if}}">Productos</a>
        <a href="/carrito" class="nav-cart-link {{#if (eq page 'carrito')}}active{{/if}}">
          Carrito 🛒 <span id="cart-count" class="badge">0</span>
        </a>
      </nav>
    </div>
  </header>

  <main class="wrap">
    {{{body}}}
  </main>

  <footer class="footer">
    <div class="wrap">Hecho por Hernán — {{year}}</div>
  </footer>
</body>
</html>
